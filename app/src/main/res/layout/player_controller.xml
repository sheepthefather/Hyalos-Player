<?xml version="1.0" encoding="utf-8"?>
<!--
  Copyright 2020 The Android Open Source Project

  Licensed under the Apache License, Version 2.0 (the "License");
  you may not use this file except in compliance with the License.
  You may obtain a copy of the License at

       http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS" BASIS,
  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  See the License for the specific language governing permissions and
  limitations under the License.
-->
<!--
  MODIFIED COPY of media3-ui's `exo_player_control_view.xml`, kept in the app so
  the transport controls can sit at the bottom instead of floating over the
  middle of the picture. Media3 has no API for moving them — a controller layout
  is the only hook — so this file is ours now, and Media3 updates will not reach
  it.

  Three departures from the original, all marked below:

    1. The transport buttons sit inside `exo_bottom_bar`, in the same row as the
       time and the settings button, rather than floating over the middle of the
       picture. The group keeps the id `exo_center_controls`, because that id is
       what `PlayerControlViewLayoutManager` fades and what it measures. It is
       `wrap_content` — with `match_parent` the group would report the screen's
       width as its own, and that measurement is what decides whether the
       controls still fit.
    2. The settings button's icon is ours. Media3's gear opens the track and
       speed menu, and the app has a gear of its own in the title bar that opens
       the app's settings; two gears in the same corner of the screen would read
       as one control.
    3. A rotate button of our own sits at the far right of the bar, past
       Media3's buttons. It is in a group of its own and *not* in
       `exo_basic_controls`, which the layout manager moves children in and out
       of at runtime — a button put there would be shuffled away with the
       overflow. For the same reason Media3's two right-hand groups are given a
       trailing margin, to make room rather than sit underneath.

  Everything else — the ids, the styles, the dimensions — is byte for byte the
  original, because `PlayerControlView` looks each one up by id and silently
  wires nothing when one is missing.
-->
<merge xmlns:android="http://schemas.android.com/apk/res/android">

  <!-- 0dp dimensions are used to prevent this view from influencing the size of
       the parent view if it uses "wrap_content". It is expanded to occupy the
       entirety of the parent in code, after the parent's size has been
       determined. See: https://github.com/google/ExoPlayer/issues/8726.
  -->
  <View android:id="@id/exo_controls_background"
      android:layout_width="0dp"
      android:layout_height="0dp"
      android:background="@color/exo_black_opacity_60"/>

  <FrameLayout android:id="@id/exo_bottom_bar"
      android:layout_width="match_parent"
      android:layout_height="@dimen/exo_styled_bottom_bar_height"
      android:layout_marginTop="@dimen/exo_styled_bottom_bar_margin_top"
      android:layout_gravity="bottom"
      android:background="@color/exo_bottom_bar_background"
      android:layoutDirection="ltr">

    <LinearLayout android:id="@id/exo_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingStart="@dimen/exo_styled_bottom_bar_time_padding"
        android:paddingEnd="@dimen/exo_styled_bottom_bar_time_padding"
        android:paddingLeft="@dimen/exo_styled_bottom_bar_time_padding"
        android:paddingRight="@dimen/exo_styled_bottom_bar_time_padding"
        android:layout_gravity="center_vertical|start"
        android:layoutDirection="ltr"
        android:importantForAccessibility="noHideDescendants">

      <TextView android:id="@id/exo_position"
          style="@style/ExoStyledControls.TimeText.Position"/>

      <TextView
          style="@style/ExoStyledControls.TimeText.Separator"/>

      <TextView android:id="@id/exo_duration"
          style="@style/ExoStyledControls.TimeText.Duration"/>

    </LinearLayout>

    <!-- The trailing margin on this group and the scroll view below is DEPARTURE
         3's other half: it makes room for the rotate button rather than letting
         Media3's buttons sit under it. One Media3 button's width, because that is
         exactly what is being placed to the right of them. -->
    <LinearLayout android:id="@id/exo_basic_controls"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical|end"
        android:layout_marginEnd="@dimen/exo_small_icon_width"
        android:layoutDirection="ltr">

      <ImageButton android:id="@id/exo_vr"
          style="@style/ExoStyledControls.Button.Bottom.VR"/>

      <ImageButton android:id="@id/exo_shuffle"
          style="@style/ExoStyledControls.Button.Bottom.Shuffle"/>

      <ImageButton android:id="@id/exo_repeat_toggle"
          style="@style/ExoStyledControls.Button.Bottom.RepeatToggle"/>

      <ImageButton android:id="@id/exo_subtitle"
          style="@style/ExoStyledControls.Button.Bottom.CC"/>

      <!-- DEPARTURE 2: our icon and wording, not Media3's gear. -->
      <ImageButton android:id="@id/exo_settings"
          style="@style/ExoStyledControls.Button.Bottom.Settings"
          android:src="@drawable/ic_tune"
          android:contentDescription="@string/player_options"/>

      <ImageButton android:id="@id/exo_fullscreen"
          style="@style/ExoStyledControls.Button.Bottom.FullScreen"/>

      <ImageButton android:id="@id/exo_overflow_show"
          style="@style/ExoStyledControls.Button.Bottom.OverflowShow"/>

    </LinearLayout>

    <HorizontalScrollView android:id="@id/exo_extra_controls_scroll_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical|end"
        android:layout_marginEnd="@dimen/exo_small_icon_width"
        android:visibility="invisible">

      <LinearLayout android:id="@id/exo_extra_controls"
          android:layout_width="wrap_content"
          android:layout_height="wrap_content"
          android:layoutDirection="ltr">

        <ImageButton android:id="@id/exo_overflow_hide"
            style="@style/ExoStyledControls.Button.Bottom.OverflowHide"/>

      </LinearLayout>

    </HorizontalScrollView>

    <!-- DEPARTURE 1. Between the time on the left and the buttons on the right,
         and centred on the screen rather than on the gap between them: those two
         are different widths, so centring on the gap would put the row slightly
         off-centre for no gain.

         Started `exo_center_controls`, so `PlayerControlViewLayoutManager` finds
         it: that id is how it fades the group, and how it measures whether the
         controls still fit. Last child, so the group draws over the rest of the
         bar — the same order it had in the original, where it came after it.

         `wrap_content`, and the padding only on the sides: the bar is 60dp and
         these buttons are 52dp, so vertical padding has nowhere to go. -->
    <LinearLayout
        android:id="@id/exo_center_controls"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:paddingStart="@dimen/exo_styled_controls_padding"
        android:paddingEnd="@dimen/exo_styled_controls_padding"
        android:layoutDirection="ltr">

      <ImageButton android:id="@id/exo_prev"
          style="@style/ExoStyledControls.Button.Center.Previous"/>

      <include layout="@layout/exo_player_control_rewind_button" />

      <ImageButton android:id="@id/exo_play_pause"
          style="@style/ExoStyledControls.Button.Center.PlayPause"/>

      <include layout="@layout/exo_player_control_ffwd_button" />

      <ImageButton android:id="@id/exo_next"
          style="@style/ExoStyledControls.Button.Center.Next"/>

    </LinearLayout>

    <!-- DEPARTURE 3: ours. A group of its own, on purpose — Media3 moves
         children in and out of `exo_basic_controls` at runtime, so a button
         placed there would be carried off with the overflow.

         Both the icon and the description are replaced from Compose as the
         screen turns, so the values here are only what the first frame shows. -->
    <LinearLayout
        android:id="@+id/player_orientation_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical|end"
        android:layoutDirection="ltr">

      <ImageButton
          android:id="@+id/player_orientation"
          style="@style/ExoStyledControls.Button.Bottom"
          android:src="@drawable/ic_orientation_landscape"
          android:contentDescription="@string/player_rotate_to_landscape" />

    </LinearLayout>

  </FrameLayout>

  <View android:id="@id/exo_progress_placeholder"
      android:layout_width="match_parent"
      android:layout_height="@dimen/exo_styled_progress_layout_height"
      android:layout_gravity="bottom"
      android:layout_marginBottom="@dimen/exo_styled_progress_margin_bottom"/>

  <LinearLayout android:id="@id/exo_minimal_controls"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_gravity="bottom|end"
      android:layout_marginBottom="@dimen/exo_styled_minimal_controls_margin_bottom"
      android:orientation="horizontal"
      android:gravity="center_vertical"
      android:layoutDirection="ltr">

    <ImageButton android:id="@id/exo_minimal_fullscreen"
        style="@style/ExoStyledControls.Button.Bottom.FullScreen"/>

  </LinearLayout>

  <LinearLayout
      android:id="@+id/exo_top_controls"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_gravity="top|end"
      android:background="@android:color/transparent">
    <View
        android:id="@+id/exo_media_route_button_placeholder"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"/>
  </LinearLayout>

</merge>
